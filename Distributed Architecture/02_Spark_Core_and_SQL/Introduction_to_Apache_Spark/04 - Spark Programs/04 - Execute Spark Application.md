Spark programs are executed by using the **spark-submit command**:
- It is a command line 
- So, it has a set of parameters
	- E.g.: name of the jar file, name of the Driver Class, the parameters of the application, etc.
## Spark-submit
It has two parameters hat are used to specify where the application is executed